# nestjs-core by Dulee

* config
* env
* db-connect
* default crud(also check nestjs-crud) -> will be develop more with custom middleware may be?
* authen & author -> will be develop with refresh token, db